# AI History - Khoa (18-04-2026)

làm và phần do AI (Copilot) hỗ trợ đề xuất/chỉnh sửa.

## Tổng quan thay đổi
- Chuyển luồng quản lý Product từ API test sang luồng MVC + Thymeleaf thực tế.
- Mở rộng service để hỗ trợ CRUD đầy đủ cho Product.
- Tích hợp upload/xoa anh Cloudinary theo flow them/sua/xoa san pham.


## 1) Phần Khoa thực hiện (chủ động)
- Xây dựng và điều chỉnh luồng Product management theo nhu cầu nghiệp vụ hiện tại.
- Cấu hình Cloudinary để lưu ảnh sản phẩm, gắn vào quá trình tạo/cập nhật dữ liệu.


## 2) Phần AI (Copilot) hỗ trợ
- Refactor `ProductController` từ kiểu API response sang MVC controller:
  - Đổi base path sang `/products`.
  - Thêm danh sách sản phẩm, xem chi tiết, form thêm/sửa, cập nhật, xóa.
  - Thêm redirect message (success/error) để cải thiện UX.
- Mở rộng `ProductService` + `ProductServiceImpl`:
  - Bổ sung `getAllProducts`, `getProductById`, `updateProduct`, `deleteProduct`.
  - Chuẩn hóa xử lý upload ảnh mới và xóa ảnh cũ trên Cloudinary khi cập nhật/xóa product.
  - Bổ sung kiểm tra logic ngày sản xuất/ngày hết hạn tại service.
- Cập nhật `ProductRequestDTO`:
  - Dùng `LocalDate` cho `manufacturerDate` và `expiryDate`.
  - Bổ sung getter/setter tường minh để ổn định binding với form multipart.
- Cập nhật `Product` entity:
  - Đổi trường ngày từ `String` sang `LocalDate` để mapping đúng kiểu dữ liệu.
- Cải tiến `ImageServiceImpl`:
  - Chuyển sang constructor injection (`@RequiredArgsConstructor`).
  - Tùy chỉnh thư mục upload Cloudinary `Legend_Coffee/Products`.
  - Xử lý an toàn cho trường hợp `publicId` null/blank hoặc ảnh không tồn tại khi xóa.
- Bổ sung `ImageException` (`@ControllerAdvice`) để xử lý lỗi upload quá dung lượng theo dạng redirect-friendly.

## 3) File chính đã thay đổi
- `src/main/java/fpt/legendcoffee/controller/ProductController.java`
- `src/main/java/fpt/legendcoffee/dto/request/ProductRequestDTO.java`
- `src/main/java/fpt/legendcoffee/entity/Product.java`
- `src/main/java/fpt/legendcoffee/service/ProductService.java`
- `src/main/java/fpt/legendcoffee/service/serviceImpl/ProductServiceImpl.java`
- `src/main/java/fpt/legendcoffee/service/serviceImpl/ImageServiceImpl.java`
- `src/main/java/fpt/legendcoffee/common/exception/ImageException.java`
- `src/main/resources/templates/product/add-product.html`
- `src/main/resources/templates/fragments/navbar.html`
- `src/main/resources/application.properties`
- `src/main/java/fpt/legendcoffee/common/config/SecurityConstants.java`
- `pom.xml`

## 4) Ghi chú kỹ thuật quan trọng
- Đang có dau hieu conflict merge chua duoc don dep trong:
  - `SecurityConstants.java`
  - `application.properties`
- Cần resolve dứt điểm conflict markers để tránh lỗi build/runtime.

## 5) Trạng thái hiện tại
- Core flow Product CRUD + upload ảnh đã được hình thành tương đối đầy đủ.
