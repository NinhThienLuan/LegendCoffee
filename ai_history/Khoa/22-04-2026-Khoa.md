# AI History - 22-04-2026 (Khoa)

<<<<<<< HEAD
=======
## Tổng kết các thay đổi và chức năng đã thực hiện:
>>>>>>> feature/wallet

### 1. Chức năng hoàn tiền (Refund)
- **Thêm mới các class, enum, repository:**
    - `RefundRequest` (entity)
    - `RefundStatus` (enum)
    - `RefundRequestRepository`
    - `WalletTransaction` (entity)
    - `WalletTransactionRepository`
- **Cập nhật OrderStatus:**
    - Thêm các trạng thái `REFUND_PROCESSING`, `REFUNDED` vào enum `OrderStatus`.
- **Triển khai service hoàn tiền:**
    - Tạo mới `RefundServiceImpl` với các chức năng:
        - User gửi yêu cầu hoàn tiền (chỉ cho phép khi đơn hàng đã thanh toán, không có yêu cầu hoàn tiền đang chờ xử lý).
        - Admin duyệt hoàn tiền: cộng tiền vào ví, lưu lịch sử giao dịch, cập nhật trạng thái đơn hàng và yêu cầu hoàn tiền.
        - Admin từ chối hoàn tiền: cập nhật trạng thái yêu cầu và trả lại trạng thái đơn hàng.
- **Tạo mới controller:**
    - `RefundController` với các API:
        - Gửi yêu cầu hoàn tiền (`/api/v1/refunds/request`)
        - Duyệt hoàn tiền (`/api/v1/refunds/{id}/approve`)
        - Từ chối hoàn tiền (`/api/v1/refunds/{id}/reject`)
- **Tạo mới DTO:**
    - `RefundRequestDTO` để nhận dữ liệu từ client.

### 2. Cập nhật liên quan ví (Wallet)
- Khi đăng ký tài khoản mới, tự động tạo ví cho user trong `AuthenServiceImpl`.
- Thêm repository cho ví (`WalletRepository`) hỗ trợ lock khi cập nhật số dư.

### 3. Tiện ích khác
<<<<<<< HEAD
- Tạo file `SecurityUtils.java` .
=======
- Tạo file `SecurityUtils.java` (chưa triển khai logic).
>>>>>>> feature/wallet

### 4. Thay đổi mới ngày 22-04-2026 (bổ sung)
- Thêm chức năng quản lý ví cho admin:
    - Tạo mới controller `WalletAdminController` và view `admin/wallets.html` để admin xem danh sách ví người dùng.
    - Thêm service `WalletService`, `WalletServiceImpl` để lấy danh sách ví.
    - Thêm link menu "Ví" vào sidebar admin (`admin-shell.html`).
- Cập nhật giao diện user:
    - Thêm nút truy cập nhanh ví và hiển thị số dư trên navbar (`navbar.html`).
- Cập nhật logic ví admin:
    - Khi thanh toán thành công (VNPay), cộng tiền vào ví admin (ID=1) trong `VNPayApplicationServiceImpl`.
    - Khi hoàn tiền, trừ tiền từ ví admin (ID=1) trong `RefundServiceImpl` (có kiểm tra số dư ví admin).
- Cập nhật repository:
    - Bổ sung method `findByIdWithLock` trong `WalletRepository` để lock ví admin khi cộng/trừ tiền.
- Cập nhật logic Refund:
    - Cho phép hoàn tiền với các trạng thái đơn hàng: CONFIRMED, SHIPPING, COMPLETED (không chỉ COMPLETED).
    - Interface `RefundService` bổ sung tham số lý do hoàn tiền (reason).

---
**Người thực hiện:** Khoa
**Ngày:** 22-04-2026
