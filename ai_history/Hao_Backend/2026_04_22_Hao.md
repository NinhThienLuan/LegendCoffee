# Nhật ký thay đổi Backend - Hao (22/04/2026)

## 1. Hệ thống Ví (Wallet System)
- **Tạo mới `WalletService` & `WalletServiceImpl`**: Tập trung hóa logic xử lý tiền tệ, bao gồm nạp tiền (`credit`), trừ tiền (`debit`) và các giao dịch đặc biệt của Admin.
- **Bảo mật giao dịch**: Sử dụng `Pessimistic Write Lock` (`findByUserIdWithLock`) để đảm bảo không xảy ra Race Condition khi cập nhật số dư ví trong môi trường đa luồng.
- **Lịch sử giao dịch**: Mọi biến động số dư đều được ghi nhận vào bảng `WalletTransaction` với đầy đủ thông tin loại giao dịch (`INCOME`, `EXPENSE`, `REFUND`) và mô tả chi tiết.

## 2. Tích hợp Thanh toán VNPay
- **Nạp tiền Admin**: Cập nhật `VNPayApplicationServiceImpl` để tự động cộng tiền vào ví Admin ngay khi nhận được IPN thanh toán thành công (`PaymentStatus.SUCCESS`).
- **Ghi chú giao dịch**: Tự động tạo mô tả giao dịch ví gắn liền với mã đơn hàng để dễ dàng đối soát.

## 3. Luồng Hủy đơn & Hoàn tiền tự động (Auto-Refund)
- **Hủy đơn linh hoạt**: Cập nhật `cancelShipping` trong `ShippingServiceImpl` để hỗ trợ hủy đơn cả khi chưa có mã vận đơn GHN (hủy nội bộ) và khi đã có mã vận đơn (hủy qua API GHN).
- **Hoàn tiền tức thì**: 
    - Khi người dùng hủy đơn thành công ở trạng thái `ready_to_pick`.
    - Hệ thống tự động chuyển trạng thái Order thành `CANCELLED` và Shipping thành `cancel`.
    - Tự động tra cứu số tiền đã thanh toán thành công và thực hiện: **Trừ ví Admin -> Cộng trả ví User**.

## 4. Đồng bộ trạng thái Webhook GHN
- **Hoàn tất đơn hàng tự động**: Cập nhật `handleWebhook` trong `ShippingServiceImpl`.
- **Logic đồng bộ**: Khi nhận được webhook trạng thái thành công từ GHN (`delivered`, `received`, `finish`), hệ thống sẽ tự động cập nhật trạng thái `Order` sang `COMPLETED`.
- **Tối ưu hóa tra cứu**: Thêm các phương thức tra cứu hiệu quả trong `UserRepository` và `PaymentRepository`.

## 5. Cấu trúc mã nguồn & Refactor
- Loại bỏ các logic xử lý ví rải rác ở các Service khác, chuyển hết về `WalletService` để dễ bảo trì.
- Clean up imports và xử lý các trường hợp null/blank cho `ghnOrderCode` để hệ thống hoạt động ổn định hơn.

---
**Người thực hiện:** Hao (Coding AI Assistant)
**Ngày cập nhật:** 22/04/2026
