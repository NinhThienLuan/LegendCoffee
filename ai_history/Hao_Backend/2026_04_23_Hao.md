# Nhật ký thay đổi Backend - Hao (23/04/2026)

## 1. Đồng bộ hóa Hoàn kho (Stock Restoration Synchronization)
- **Tối ưu luồng Hủy vận chuyển**:
    - Cập nhật `ShippingServiceImpl.java` để sử dụng `OrderService.cancelOrder` thay vì chỉ cập nhật trạng thái đơn hàng thủ công.
    - Đảm bảo khi hủy đơn qua API GHN hoặc Webhook, hệ thống sẽ tự động gọi logic hoàn kho để cộng lại sản phẩm.
- **Xử lý Thanh toán thất bại**:
    - Cập nhật `VNPayApplicationServiceImpl.java` để thực hiện hủy đơn và hoàn kho **ngay lập tức** khi nhận được thông báo thanh toán thất bại (người dùng hủy trên cổng VNPay hoặc lỗi thẻ).
    - Giúp tránh việc giữ hàng ảo trong thời gian chờ Schedule Job quét.

## 2. Củng cố tính ổn định của hệ thống
- **Quản lý Dependency**:
    - Tiêm (`Inject`) `OrderService` vào `ShippingServiceImpl` và `VNPayApplicationServiceImpl` để tái sử dụng logic nghiệp vụ cốt lõi.
    - Xử lý các lỗi trùng lặp import và tối ưu hóa constructor.
- **Tính nhất quán**:
    - Đảm bảo tất cả 4 luồng hủy đơn (Thủ công, Webhook, Lỗi thanh toán, và Job tự động) đều hoạt động đồng nhất về mặt trạng thái và kho hàng.

---
**Người thực hiện:** Hao (Coding AI Assistant)
**Ngày cập nhật:** 23/04/2026
