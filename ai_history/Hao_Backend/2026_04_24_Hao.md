# Lịch sử thay đổi Backend - Hao (23/04/2026)

Dưới đây là tổng hợp toàn bộ các thay đổi và cập nhật mà bạn (Hào) đã thực hiện trong dự án vào ngày hôm qua (23/04/2026).

## 1. Hoàn thiện toàn bộ luồng rút tiền (Withdrawal Flow) cho User
- **Tính năng:** Đã xây dựng hoàn chỉnh luồng yêu cầu rút tiền từ ví của người dùng.
- **Chi tiết thực hiện:**
    - Tạo bảng và entity mới `WithdrawalRequest` kèm theo file script database `migration_withdrawal.sql`.
    - Thêm Enum `WithdrawalStatus` để quản lý trạng thái của yêu cầu rút tiền.
    - Xây dựng các lớp `WithdrawalRequestDTO`, `WithdrawalRequestRepository`, `WithdrawalService` và phần implementation `WithdrawalServiceImpl`.
    - Cập nhật `WalletController` với các API endpoint để xử lý việc tạo request rút tiền, duyệt/hủy request.
    - Sửa cấu hình `SecurityConfig` để phân quyền cho các luồng mới.
- **Commit:** `d3087d7` - *feat(witdraw): complete flow request withdraw*

## 2. Sửa lỗi trừ tiền ví Admin 2 lần khi Approve
- **Lỗi:** Khi Admin tiến hành duyệt (approve) yêu cầu rút tiền của người dùng, hệ thống đã trừ tiền trong ví Admin đến 2 lần.
- **Cách khắc phục:** Sửa đổi logic trong `WithdrawalServiceImpl.java`, thay đổi/comment lại đoạn code trừ tiền bị lặp để đảm bảo ví Admin chỉ bị trừ đúng 1 lần với số tiền tương ứng.
- **Commit:** `11f97f3` - *fix(withdrawal: sub amount admin twice*

## 3. Refactor luồng Thanh toán & Giao hàng (Checkout & GHN)
- **Tính năng:** Loại bỏ số "magic number" và thiết lập giá trị thanh toán mặc định.
- **Chi tiết thực hiện:**
    - Cập nhật `CheckoutRequestDTO` để set mặc định ID loại thanh toán.
    - Thêm Enum `GhnPaymentTypeId` và dùng Enum này để thay thế cho các số cứng (magic numbers) trong `ShippingServiceImpl`.
- **Commit:** `53196f4` - *fix(checkout): set default paymentTypeId and replace magic numbers with Enum*

## Tổng kết
Hôm qua, bạn đã tập trung chủ yếu vào nhánh `feat/withdraw` để hoàn thành tính năng **rút tiền ví** cực kỳ quan trọng, từ việc dựng cấu trúc database, logic service, cho đến xử lý triệt để bug trừ đúp tiền của hệ thống (Admin). Bên cạnh đó, bạn cũng đã refactor lại cách truyền tham số thanh toán cho GHN qua Enum gọn gàng và an toàn hơn.
