# Default profile (local run)
# Uses in-memory H2 by Spring Boot defaults unless overridden.

#Cloudinary configuration
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:myimagename}
cloudinary.api-key=${CLOUDINARY_API_KEY:255142222551169}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:OoKkAH6Ve_DaiOXBCfAGEy51HgI}
# Upload limit
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

# JPA/Hibernate (shared defaults)
spring.jpa.open-in-view=false
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true

# SQL init scripts (set to always if you add schema.sql/data.sql)
spring.sql.init.mode=${SQL_INIT_MODE:never}

# Logging
logging.level.root=${LOG_LEVEL_ROOT:INFO}
logging.level.org.springframework.web=${LOG_LEVEL_WEB:INFO}
logging.level.org.hibernate.SQL=${LOG_LEVEL_HIBERNATE_SQL:WARN}
logging.level.org.hibernate.orm.jdbc.bind=${LOG_LEVEL_HIBERNATE_BIND:WARN}

# Mail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:luanninh2005@gmail.com}
spring.mail.password=${MAIL_PASSWORD:mjtj mzzs czpy fdmb}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.thymeleaf.cache=false 
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
